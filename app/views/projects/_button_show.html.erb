<% if policy(project).show? %>
  <%= link_to project_path(project), class: "my-1 ml-3 mr-sm-2", data: {toggle: "tooltip"}, title:"Show project" do %>
    <i class="fas fa-eye fa-fw"></i>
  <% end %>
<% end %>
