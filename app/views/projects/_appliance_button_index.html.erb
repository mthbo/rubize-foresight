<% if policy(ProjectAppliance).create? %>
  <%= link_to appliances_project_path(project), class: "btn btn-outline-secondary flex-grow-1" do %>
    <i class="fas fa-plus mr-1"></i>
    Add an appliance to the project
  <% end %>
<% end %>
