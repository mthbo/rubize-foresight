<% if policy(project).destroy? %>
  <%= link_to project_path(project), method: :delete, data: {confirm: 'Are you certain you want to delete this project?'}, class: "btn btn-outline-danger" do %>
    <i class="fas fa-trash-alt mr-1"></i>
    Delete
  <% end %>
<% end %>
