<% if appliance.photo.present? %>
  <%= cl_image_tag appliance.photo, width: size, height: size, crop: :pad %>
<% else %>
  <%= image_tag "appliances_square.svg", alt: "Appliance", width: size %>
<% end %>
