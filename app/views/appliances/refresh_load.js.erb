<% if @use.present? %>
  <% (0..23).each do |hour| %>
    $('#appliance_hourly_rate_' + <%= hour %> ).val('<%= @use.method("hourly_rate_#{hour}").call %>');
    $('#appliance_hourly_rate_' + <%= hour %> ).barrating('set', '<%= @use.method("hourly_rate_#{hour}").call %>');
  <% end %>
<% end %>
