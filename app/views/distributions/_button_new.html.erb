<% if policy(Distribution).create? %>
  <%= link_to new_distribution_path, class: "btn btn-outline-secondary flex-grow-1" do %>
    <i class="fas fa-plus mr-1"></i>
    New electrical distribution
  <% end %>
<% end %>
