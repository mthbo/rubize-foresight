<div class="list-group-item d-flex justify-content-between align-items-center" data-distribution-id="<%= distribution.id %>">

  <div>
    <h6>
      <i class="fas fa-plug fa-fw mr-1"></i>
      Electrical distribution (per appliance).
    </h6>
    <div class="mb-2">
      <%= render 'distributions/badge_price', distribution: distribution %>
    </div>
  </div>

  <div class="flex-shrink-0">
    <%= render 'distributions/button_edit', distribution: distribution %>
  </div>

</div>
