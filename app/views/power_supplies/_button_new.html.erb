<% if policy(PowerSupply).create? %>
  <%= link_to new_project_power_supply_path(project), class: "btn btn-sm btn-outline-primary flex-grow-1" do %>
    <i class="fas fa-plus mr-1"></i>
    Add a solar system
  <% end %>
<% end %>
