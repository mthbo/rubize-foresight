<% unless battery.currency == 'eur' %>
  <div class="badge badge-secondary px-2 mr-2">
    <%= "#{battery.price.format(no_cents: true, symbol: false, thousands_separator: " ")} #{battery.price.currency}" %>
  </div>
<% end %>
