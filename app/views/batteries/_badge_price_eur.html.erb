<% if battery.price_eur.present? %>
  <div class="badge badge-dark px-2">
    <%= "#{battery.price_eur.format(no_cents: true, symbol: false, thousands_separator: " ")} #{battery.price_eur.currency}" %>
  </div>
<% end %>
