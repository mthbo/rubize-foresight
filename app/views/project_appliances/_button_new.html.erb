<% if policy(ProjectAppliance).new? %>
  <%= link_to new_project_project_appliance_path(project, appliance_id: appliance.id), class: "btn btn-outline-secondary btn-sm flex-grow-1" do %>
    <i class="fas fa-plus mr-1"></i>
    Add
  <% end %>
<% end %>
